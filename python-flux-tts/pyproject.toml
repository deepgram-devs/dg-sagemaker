[project]
name = "python-flux-tts"
version = "0.1.0"
description = "Deepgram SageMaker Flux TTS (/v2/speak) e2e + stress client"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-sdk-sagemaker-runtime-http2",
    "boto3>=1.42.34",
]
