[project]
name = "python-flux"
version = "0.1.0"
description = "Deepgram Flux STT SageMaker stress test"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-sdk-sagemaker-runtime-http2[awscrt]>=0.11",
    "boto3>=1.42.34",
    "pyaudio>=0.2.14",
]

# The client is on PyPI now (>= 0.11); the git source predates that.
# [tool.uv.sources]
# aws-sdk-sagemaker-runtime-http2 = { git = "https://github.com/awslabs/aws-sdk-python", subdirectory = "clients/aws-sdk-sagemaker-runtime-http2" }
